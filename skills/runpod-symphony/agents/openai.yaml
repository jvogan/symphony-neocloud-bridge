interface:
  display_name: "RunPod Symphony"
  short_description: "RunPod lane checks for Symphony"
  default_prompt: "Use $runpod-symphony for Linear-backed RunPod work: local dry-run first; require remote_launch_allowed, live productivity proof for nontrivial runs, artifact validation, and cleanup before success."
